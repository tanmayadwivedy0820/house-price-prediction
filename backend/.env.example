# Copy this file to .env and set PROJECT_ROOT to your absolute backend path.
PROJECT_ROOT=/absolute/path/to/house-price-prediction/backend
DATASET_DIR=dataset
DATASET_NAME=housing.csv
MODEL_DIR=model_dir
MODEL_NAME=house_price_model.joblib
TARGET_COL=median_house_value
RANDOM_STATE=42

# API host the frontend will call (used later by main.py if needed)
API_HOST=0.0.0.0
API_PORT=8000
